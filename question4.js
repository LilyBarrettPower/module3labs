let a = 2, b = 3;
let result = `${a} + ${b} is `;
if (a + b < 10) {
result += 'less than 10';
} else {
result
}
console.log(result)


// Re-write this code using the ternary/conditional operator ?

let c = 2, d = 3;
let answer = `${a} + ${b} is `;
(c + d < 10)? answer+= 'less than 10': answer

console.log(answer)
// What does the += do?
// This is the addition assignment operator which does addition on the two perands and assigns the result to the left operand 